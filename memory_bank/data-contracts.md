
**核心数据库表结构(ER图)、公共API的请求/响应JSON格式（写代码前必读）**

# 数据库核心实体 (ER 结构)
[要求：使用 Mermaid 语法绘制或清晰的 Markdown 表格]

# 核心 API 规范
- 所有 API 请求响应均需遵循以下标准格式：
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}